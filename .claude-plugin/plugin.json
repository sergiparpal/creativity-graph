{
  "name": "creativity-graph",
  "version": "0.3.3",
  "description": "Turn a non-self-grounding conceptual document into a rigorously-grounded, queryable knowledge graph: a human-editable canon with three-axis provenance, a span-present write boundary, a grounding loop with memory of failures, and a regenerable NetworkX/SQLite derived layer.",
  "author": {
    "name": "Sergi Parpal",
    "email": "sergiparpal@gmail.com"
  },
  "license": "MIT",
  "keywords": ["knowledge-graph", "grounding", "provenance", "networkx", "mcp"],
  "userConfig": {
    "source_path": {
      "type": "file",
      "title": "Source document path",
      "description": "Path to the source the graph is built and grounded against — a single Markdown/text file, or a directory or glob of .md/.txt files (each edge's span is verified against the specific file it came from). Markdown/text only — no PDF/media."
    },
    "sensitivity": {
      "type": "string",
      "title": "Egress sensitivity",
      "description": "Egress scrubbing aggressiveness — one of low | medium | high (low = secrets only; medium = + structured PII; high = + person/address heuristics). Validated by the engine; unknown values fall back to medium.",
      "default": "medium"
    },
    "metrics_mode": {
      "type": "string",
      "title": "Metrics mode",
      "description": "One of structure_only | with_embeddings (structure_only uses graph structure as the signal; with_embeddings is currently inert — the former sqlite-vss candidate generator was removed). Validated by the engine; unknown values fall back to structure_only.",
      "default": "structure_only"
    }
  }
}
